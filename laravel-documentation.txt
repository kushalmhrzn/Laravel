installation:
1. firstly install composer, and xampp
2. composer la laravel installation using command
"composer global require laravel/installer
"
3. new project bhanauna "laravel new {project name withoutspace}"
